import DeleteButton from '@/components/commons/ui-delete-button/DeleteButton';

export default function Home() {
  return (
    <div>
      랜딩 페이지임
      <DeleteButton onClick={() => {}} />
    </div>
  );
}
